// Tuple
//Tuples can contain two values of different types

let tuple: [string, number, boolean] = ["hello", 43, true]

let details: [string, number, boolean];
details = ["alex", 23, true]
details[0] // returns "alex"
details[1] // returns 23
// you can add new elements to a tuple using the push method
const employee: [number, string] = [1, "adam"]
employee.push(2, "alex")
console.log(employee);


